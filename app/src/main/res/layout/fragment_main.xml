<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/content_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:gravity="top"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingBottom="@dimen/activity_vertical_margin"
        tools:context="com.intern.zappos.Fragments.MainActivityFragment">

    <EditText
            android:id="@+id/edit_query"
            android:layout_width="match_parent"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:layout_height="wrap_content"
            android:hint="Enter Search Query Here..."/>
    <Button android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/edit_query"
            android:text="search"
            android:id="@+id/search_button_local"/>
    <ProgressBar
            android:id="@+id/progress"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@+id/search_button_local"
            android:layout_alignBottom="@+id/search_button_local"
            android:paddingBottom="@dimen/product_title_top_margin"
            android:layout_centerHorizontal="true"
            android:indeterminate="true"
            android:elevation="2dp"
            android:indeterminateTint="@android:color/holo_blue_dark"
            android:visibility="gone" />


</RelativeLayout>
